server:
  port: 8082

spring:
  application:
    name: financeiro
  datasource:
    url: jdbc:postgresql://localhost:5433/financeiro-db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultzone: http://localhost:8761/eureka
