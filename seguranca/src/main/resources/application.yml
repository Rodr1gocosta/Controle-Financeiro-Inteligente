spring:
    application:
        name: seguranca
    profiles:
        active: dev

management:
    endpoints:
        web:
            exposure:
                include: health
    endpoint:
        health:
            show-details: always
